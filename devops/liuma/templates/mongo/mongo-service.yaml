apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongo-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-mongo
  ports:
    - port: 27017
      targetPort: 27017